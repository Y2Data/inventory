DATABASE_URL=postgresql://user:password@host/database?sslmode=require
INVENTORY_PASSWORD=replace-with-a-random-password-at-least-12-characters
SESSION_SECRET=replace-with-at-least-32-random-characters
# Auto-provisioned once a Vercel Blob store is created and linked to this
# project (Vercel dashboard/Marketplace). Pull locally with `vercel env pull`.
BLOB_READ_WRITE_TOKEN=
